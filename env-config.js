// LOCAL DEVELOPMENT FALLBACK — overwritten automatically by `npm run build`.
// Do not put real keys here. This file exists only so that opening
// index.html directly (without running the build step) doesn't throw
// a hard error — the donation form will simply show a clear configuration
// message instead of crashing.
window.__SUPABASE_ENV__ = {
  url: "",
  publishableKey: ""
};
